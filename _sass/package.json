{
  "scripts": {
    "serve": "cross-env NODE_ENV=development concurrently \"postcss src/style.css -o ../assets/css/style.css --watch\"",
    "start": "npm run serve"
  },
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "postcss-import": "^12.0.1",
    "postcss-nested": "^4.2.3",
    "tailwindcss": "^1.7.2"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.2",
    "cssnano": "^4.1.10",
    "postcss-cli": "^7.1.1"
  }
}
